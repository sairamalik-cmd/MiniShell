# MiniShell
MiniShell is a Linux command-line interface (CLI) shell in C++ supporting built-in commands and external program execution. It illustrates OS concepts like fork, execvp, and file/directory operations.
